spring:
  application:
    name: Person-service

  datasource:
    # url: jdbc:mysql://localhost:3307/persondb
    url: jdbc:mysql://mysql:3306/persondb
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081
  # port: 60901

id: instance-1